describe('Accordian', () => {
    
    beforeEach(()=>{
        cy.visit(`${Cypress.env("demoQA")}/accordian`, {timeout: 20000});
    });

    it('Default Accordion validation example', () => {
        
    });


});